# 出口 WebSocket 服务监听
WS_LISTEN_HOST=0.0.0.0
WS_LISTEN_PORT=8765

# 真实 Minecraft 目标（通常是本机或内网的游戏服/代理，如 ZBProxy 等）
MC_TARGET_HOST=127.0.0.1
MC_TARGET_PORT=25565

# 认证密钥（请修改）
AUTH_TOKEN=zzyss666

# 心跳/超时/块大小调优
PING_INTERVAL=60
PING_TIMEOUT=20
TCP_CONNECT_TIMEOUT=10
CHUNK=16384
